apiVersion: v2
name: agent-devstack
description: Dev-only extras for running the Agent stack on a local cluster; never deployed to shared clusters
type: application
version: 1.0.0
appVersion: "1.0.0"
